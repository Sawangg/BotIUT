const Discord = require("discord.js");
const botconfig = require("../util/config.json");

module.exports = async (reaction, user) => {

    // if ((reaction.message.channel.id === "744212634446224976") && (reaction.emoji.name === '🚩')) {
    //         reaction.message.guild.members.cache.get(user.id).roles.add('746263081695067178').catch()
    // }

    // if(reaction.emoji.id == "💚" && reaction.message.id === "749564004695015474" && reaction.user !== bot.id) {
    //     console.log("oh!")
    //     guild.fetchMember(user).then((member) => {
    //         let role = member.guild.roles.find(role => role.name === "G1");
    //         member.addRole(role)
    //     });
    // }
}